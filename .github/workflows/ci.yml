name: CI

on:
  pull_request:
    branches: [main, dev]
  push:
    branches: [main, dev]

jobs:
  backend-tests:
    name: Backend Lint & Tests
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: initiative
          POSTGRES_PASSWORD: initiative
          POSTGRES_DB: initiative_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U initiative"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql+asyncpg://initiative:initiative@localhost:5432/initiative_test
      DATABASE_URL_APP: postgresql+asyncpg://app_user:app_user@localhost:5432/initiative_test
      DATABASE_URL_ADMIN: postgresql+asyncpg://app_admin:app_admin@localhost:5432/initiative_test
      SECRET_KEY: ${{ secrets.CI_SECRET_KEY }}
      AUTO_APPROVED_EMAIL_DOMAINS: '["example.com"]'

    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 0

      - name: Check for backend changes
        id: changes
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            CHANGED=$(git diff --name-only origin/${{ github.base_ref }}...HEAD -- backend/ || true)
          else
            CHANGED=$(git diff --name-only HEAD~1...HEAD -- backend/ || true)
          fi

          if [[ -z "$CHANGED" ]]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "No backend changes — skipping"
            exit 0
          fi

          echo "skip=false" >> $GITHUB_OUTPUT

          # Check if shared infra changed (requires full test run)
          if echo "$CHANGED" | grep -qE '(app/testing/|app/models/|app/schemas/|app/core/|app/db/|conftest\.py)'; then
            echo "scope=app/" >> $GITHUB_OUTPUT
            echo "Shared infrastructure changed — running all tests"
          else
            # Extract unique directories, strip backend/ prefix, and exclude non-code dirs
            DIRS=$(echo "$CHANGED" | sed 's|^backend/||' | grep -v '^app/locales/' | xargs -I{} dirname {} | sort -u | tr '\n' ' ')
            if [[ -z "${DIRS// }" ]]; then
              echo "scope=app/" >> $GITHUB_OUTPUT
              echo "Only non-code files changed — running all tests"
            else
              echo "scope=$DIRS" >> $GITHUB_OUTPUT
              echo "Running tests in: $DIRS"
            fi
          fi

      - name: Set up Python
        if: steps.changes.outputs.skip != 'true'
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        if: steps.changes.outputs.skip != 'true'
        run: |
          cd backend
          pip install -r requirements.txt
          pip install pytest pytest-asyncio pytest-cov ruff

      - name: Run Alembic migrations
        if: steps.changes.outputs.skip != 'true'
        run: |
          cd backend
          alembic upgrade head

      - name: Lint with Ruff
        if: steps.changes.outputs.skip != 'true'
        run: |
          cd backend
          ruff check app

      - name: Run tests
        if: steps.changes.outputs.skip != 'true'
        run: |
          cd backend
          python -m pytest ${{ steps.changes.outputs.scope }} -v --tb=short

  frontend-lint-and-test:
    name: Frontend Lint & Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 0

      - name: Check for frontend changes
        id: filter
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            CHANGED=$(git diff --name-only origin/${{ github.base_ref }}...HEAD -- frontend/ || true)
          else
            CHANGED=$(git diff --name-only HEAD~1...HEAD -- frontend/ || true)
          fi
          if [[ -z "$CHANGED" ]]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "No frontend changes — skipping"
          else
            echo "skip=false" >> $GITHUB_OUTPUT
            echo "Frontend changes detected"
          fi

      - name: Set up Node.js
        if: steps.filter.outputs.skip != 'true'
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: "24"

      - name: Install pnpm
        if: steps.filter.outputs.skip != 'true'
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5
        with:
          version: 9

      - name: Install dependencies
        if: steps.filter.outputs.skip != 'true'
        run: |
          cd frontend
          pnpm install --frozen-lockfile

      - name: Lint
        if: steps.filter.outputs.skip != 'true'
        run: |
          cd frontend
          pnpm lint

      - name: TypeScript Check
        if: steps.filter.outputs.skip != 'true'
        run: |
          cd frontend
          pnpm tsc --noEmit

      - name: Run tests
        if: steps.filter.outputs.skip != 'true'
        run: |
          cd frontend
          pnpm vitest run --passWithNoTests

  check-generated-types:
    name: Check Generated Types
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 0

      - name: Check for backend changes
        id: filter
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            CHANGED=$(git diff --name-only origin/${{ github.base_ref }}...HEAD -- backend/ frontend/orval.config.ts frontend/src/api/mutator.ts || true)
          else
            CHANGED=$(git diff --name-only HEAD~1...HEAD -- backend/ frontend/orval.config.ts frontend/src/api/mutator.ts || true)
          fi
          if [[ -z "$CHANGED" ]]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "No backend or codegen config changes — skipping"
          else
            echo "skip=false" >> $GITHUB_OUTPUT
            echo "Backend or codegen config changes detected"
          fi

      - name: Set up Python
        if: steps.filter.outputs.skip != 'true'
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install backend dependencies
        if: steps.filter.outputs.skip != 'true'
        run: |
          cd backend
          pip install -r requirements.txt

      - name: Export OpenAPI spec
        if: steps.filter.outputs.skip != 'true'
        run: |
          cd backend
          python scripts/export_openapi.py ../frontend/openapi.json

      - name: Set up Node.js
        if: steps.filter.outputs.skip != 'true'
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: "24"

      - name: Install pnpm
        if: steps.filter.outputs.skip != 'true'
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5
        with:
          version: 9

      - name: Install frontend dependencies
        if: steps.filter.outputs.skip != 'true'
        run: |
          cd frontend
          pnpm install --frozen-lockfile

      - name: Regenerate types
        if: steps.filter.outputs.skip != 'true'
        run: |
          cd frontend
          pnpm orval
          pnpm prettier --write src/api/generated/

      - name: Check for drift
        if: steps.filter.outputs.skip != 'true'
        run: |
          if ! git diff --exit-code frontend/src/api/generated/; then
            echo ""
            echo "ERROR: Generated types are out of date!"
            echo "Run 'pnpm generate:api' in the frontend directory and commit the results."
            echo ""
            exit 1
          fi
          echo "Generated types are up to date."
